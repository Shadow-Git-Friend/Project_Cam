# Project_Cam runtime dependencies.
# Frozen versions are mirrored in requirements.lock.txt (generated via pip freeze).
#
# Voice bridge (vosk + pyaudio) lives in a separate venv per the multi-venv
# architecture documented in CLAUDE.md (2026-04-17 Session Log entry). Do NOT
# add pyaudio/vosk here — voice_bridge.py runs under colleague's venv at
# /home/hanush/Desktop/Speech\ to\ text/.

# Core numerics + IO
numpy==1.26.4
opencv-python==4.12.0.88
PyYAML==6.0.3

# Visualization
matplotlib==3.10.7
Jinja2==3.1.6

# Optional live mesh avatar / SMPL fitting dependencies live in
# requirements-avatar.txt. Install them only on machines that will run
# `--smpl-avatar`, because the body model stack is large and CUDA-sensitive.

# Biomechanics export (C3D for Visual3D / Mokka / OpenSim partnerships)
ezc3d>=1.5,<2.0

# Vision models
ultralytics==8.3.229
onnx==1.19.1

# MMPose backend (fallback to YOLO-Pose primary)
mmpose==1.3.2
mmcv==2.1.0
mmengine==0.10.7

# Hardware I/O
pyserial==3.5
