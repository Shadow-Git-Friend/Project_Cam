# Optional avatar / SMPL mesh-fitting dependencies.
#
# Install this only on machines where you want to run `--smpl-avatar`.
# SMPL/SMPL-X model files are licensed separately and must be downloaded from
# the official model providers; this requirements file does not include them.
#
# If CUDA wheels matter on your machine, install torch/torchvision from the
# official PyTorch selector first, then install the rest of this file.

torch
torchvision
scipy>=1.11
smplx
trimesh[easy]
pyrender
PyOpenGL
PyOpenGL_accelerate

# Optional later/offline tooling:
# open3d
