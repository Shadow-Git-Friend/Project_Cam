# Lightweight dependency set for the API service container (CPU, no GPU stack).
# This is the aim-only service surface: health, system info, cameras,
# triangulation, prediction, session-report contract, and /metrics.
#
# The heavy inference stack (torch/ultralytics/mmpose/TensorRT) is NOT here --
# GPU inference runs in the separate GPU image / live rig, not the API container.

numpy>=1.26,<2.0
PyYAML>=6.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
prometheus-client>=0.20
python-multipart>=0.0.9
# project_cam.geometry.triangulation imports cv2 at module load, which the API
# import chain (pipeline_adapter -> geometry) reaches on startup. Without this
# the Docker CPU container crashes at boot with ModuleNotFoundError: cv2.
# Headless = no GUI libs (the image still installs libgl1/libglib2.0-0). Capped
# below 4.13 because 4.13 requires numpy>=2, which conflicts with numpy<2 above.
opencv-python-headless>=4.10,<4.13
