# Data

Local assessment inputs and reports live here:

- `raw/`: UDP joint recordings and imported source files.
- `processed/`: cleaned or converted motion files.
- `reports/`: generated athlete assessment JSON reports.

Do not store real child names in filenames or report metadata.

