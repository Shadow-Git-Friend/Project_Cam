{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions. core:default is required for the event API (listen/emit) — without it the UI silently never receives pipeline-log/pipeline-exit events and the RUNNING state gets stuck.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
